{
  "name": "lunasomnia-ephemeris-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  },
  "dependencies": {
    "@fusionstrings/swisseph-wasi": "0.1.0"
  }
}
